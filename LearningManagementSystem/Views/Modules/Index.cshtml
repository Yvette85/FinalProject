@model LearningManagementSystem.Models.IndexModuleViewModels
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Modules[0].Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Modules[0].Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Modules[0].Start)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Modules[0].End)
        </th>
      <th>
          @Html.DisplayNameFor(model=>model.Modules[0].Courses[0].Name)
      </th>
        <th></th>
        <th></th>
    </tr>

@foreach (var item in Model.Modules) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Start)
        </td>
        <td>
            @Html.DisplayFor(ModelItem => item.End)
        </td>
        <td>
            @Html.DisplayFor(Modelitem => item.Courses[0].Name)
        </td>
        <td>
             @Html.DisplayFor(ModelItem => item.Courses)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
